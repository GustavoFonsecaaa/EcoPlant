import 'package:flutter/material.dart';

class JardimScreen extends StatelessWidget {
const JardimScreen({Key? key }) : super(key: key);


@override
Widget build(BuildContext context){
return const Scaffold(
body: Center(child: Text('Jardim'),),
);
}
}